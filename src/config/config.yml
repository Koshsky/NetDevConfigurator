database:
  host: localhost            # by default
  port: 5432                 # by default
  database: device_registry  # by default
  username: postgres         # by default
  password: postgres         # by default

tftp-server:
  address: 10.4.1.144
  port: 69
  folder: /srv/tftp

host:
  username: mvsadmin
  password: MVS_admin
  address: 10.4.0.3
  port: 22


app:
  geometry: "1400x1000+300+100"
  templates:
    width: 6
    allow-none: True
  interfaces:
    width: 12
    allow-none: False

default-cert: MVS
serial-port: /dev/ttyUSB0
ssh-config-file: ~/NetDevConfigurator/src/config/ssh.conf  # unused in last versions
backup-folder: ./backups
